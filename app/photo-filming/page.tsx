import type { Metadata } from "next"
import PhotoFilmingClientPage from "./PhotoFilmingClientPage"

export const metadata: Metadata = {
  title: "Photo & Filming – Roy-André",
}

export default function PhotoFilmingPage() {
  return <PhotoFilmingClientPage />
}
